WORK IN PROGRESS

1. This application is a password manager. That also encrypts when a note is saved.

2. On first time use the user will be asked to set a 
  4-digit PIN code. This is used to log in to the application.

3. After a PIN is set 

3. When a user select the choice to make a new note
  they get a form that will store what they write.

The form contains a field for
-Title:
-Note:
-Email:
-Password:
-NB:

When a user saves the note it will be encrypted..
